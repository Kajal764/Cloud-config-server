eureka:
  client:
    register-with-eureka: true
    fetch-registry: true
    service-url:
      defaultZone: http://localhost:8761/eureka/
  instance:
    localhost
    
 microservice:
  user-service:
    endpoints:
      endpoint:
        uri: http://user-service/fundoo/user/id/
